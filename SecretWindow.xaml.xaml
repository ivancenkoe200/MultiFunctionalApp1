<Window x:Class="MultiFunctionalApp_.SecretWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Секретное сообщение" Height="200" Width="300">
    <StackPanel>
        <PasswordBox Name="PasswordInput" Width="200" Margin="10" />
        <Button Name="SubmitButton" Content="Ввести пароль" Click="SubmitButton_Click" Margin="10" />
        <TextBlock Name="MessageText" Text="" Margin="10" />
    </StackPanel>
</Window>